builddir=../../../builder/componentbuild
srcdir=../..

# Path to top level build directory if different
# from module name. e.g. If yahoo_bootstrap.js needs to 
# reside in 3.x/build/bootstrap as opposed to 3.x/build/yahoo_bootstrap
global.build.component=${srcdir}/build/bootstrap
component=yahoo_bootstrap
component.jsfiles=yahoo_bootstrap.js
register.skip=true
clean.skip=false
