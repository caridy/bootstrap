builddir=../../../builder/componentbuild

component=bootstrap
component.rollup=true

# Path to sub-module build files
component.rollup.modules.buildfiles=build.yui.xml, build.yahoo.xml

register.skip=true
clean.skip=false