builddir=../../../builder/componentbuild
srcdir=../..

# Path to top level build directory if different
# from module name. e.g. If yui_bootstrap.js needs to 
# reside in 3.x/build/bootstrap as opposed to 3.x/build/yui_bootstrap
global.build.component=${srcdir}/build/bootstrap
component=yui_bootstrap
component.jsfiles=yui_bootstrap.js
register.skip=true
clean.skip=false
